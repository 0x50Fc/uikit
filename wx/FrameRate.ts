
namespace wx{
    /**
     * 可以修改渲染帧率。默认渲染帧率为 60 帧每秒。修改后，requestAnimationFrame 的回调频率会发生改变。
     * @param fps {number}  帧率，有效范围 1 - 60。
     */
    export function setPreferredFramesPerSecond(fps:number){}
}